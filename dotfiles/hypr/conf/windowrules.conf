# ── Lune OS — Window Rules (Sintaxe v0.54) ──────────────────

# ── Janelas flutuantes ───────────────────────────────────────
<<<<<<< HEAD
windowrule = float on, match:class ^(pavucontrol)$
windowrule = float on, match:class ^(blueman-manager)$
windowrule = float on, match:class ^(nm-connection-editor)$
windowrule = float on, match:class ^(org.gnome.Calculator)$
windowrule = float on, match:class ^(gnome-calculator)$
windowrule = float on, match:class ^(file-roller)$
windowrule = float on, match:class ^(org.gnome.FileRoller)$
windowrule = float on, match:title ^(Abrir arquivo)$
windowrule = float on, match:title ^(Salvar arquivo)$
windowrule = float on, match:title ^(Salvar como)$
windowrule = float on, match:title ^(Open File)$
windowrule = float on, match:title ^(Save File)$

# ── Tamanhos ─────────────────────────────────────────────────
windowrule = size 800 600, match:class ^(pavucontrol)$
windowrule = size 900 650, match:class ^(blueman-manager)$
windowrule = size 500 400, match:class ^(gnome-calculator)$
windowrule = size 500 400, match:class ^(org.gnome.Calculator)$

# ── Centralizar ──────────────────────────────────────────────
windowrule = center on, match:class ^(pavucontrol)$
windowrule = center on, match:class ^(blueman-manager)$
windowrule = center on, match:class ^(gnome-calculator)$
windowrule = center on, match:class ^(org.gnome.Calculator)$

# ── Opacidade ────────────────────────────────────────────────
windowrule = opacity 0.95 0.85, match:class ^(kitty)$
windowrule = opacity 0.98 0.95, match:class ^(Code)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(google-chrome)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(vlc)$

# ── Workspace por app ────────────────────────────────────────
windowrule = workspace 2, match:class ^(google-chrome)$
windowrule = workspace 3, match:class ^(Code)$
windowrule = workspace 4, match:class ^(discord)$
windowrule = workspace 4, match:class ^(telegram-desktop)$
windowrule = workspace 5, match:class ^(Spotify)$

# ── Fix XWayland & Misc ──────────────────────────────────────
windowrule = suppress_event maximize, match:class .*
windowrule = float on, match:class ^(wine)$

# ── Layer Rules (v0.54 exige sintaxe de namespace) ──────────
layerrule = blur on, waybar
layerrule = ignore_alpha 0.3, waybar
layerrule = blur on, rofi
layerrule = ignore_alpha 0.3, rofi
layerrule = blur on, swaync-control-center
layerrule = ignore_alpha 0.3, swaync-control-center
=======

windowrule = float on, match:class ^(pavucontrol)$ windowrule = float on, match:class ^(blueman-manager)$ windowrule = float on, match:class ^(nm-connection-editor)$ windowrule = float on, match:class ^(org.gnome.Calculator)$ windowrule = float on, match:class ^(gnome-calculator)$ windowrule = float on, match:class ^(file-roller)$ windowrule = float on, match:class ^(org.gnome.FileRoller)$ windowrule = float on, match:title ^(Abrir arquivo)$ windowrule = float on, match:title ^(Salvar arquivo)$ windowrule = float on, match:title ^(Salvar como)$ windowrule = float on, match:title ^(Open File)$ windowrule = float on, match:title ^(Save File)$

# ── Tamanhos ─────────────────────────────────────────────────

windowrule = size 800 600, match:class ^(pavucontrol)$ windowrule = size 900 650, match:class ^(blueman-manager)$ windowrule = size 500 400, match:class ^(gnome-calculator)$ windowrule = size 500 400, match:class ^(org.gnome.Calculator)$

# ── Centralizar ──────────────────────────────────────────────

windowrule = center on, match:class ^(pavucontrol)$ windowrule = center on, match:class ^(blueman-manager)$ windowrule = center on, match:class ^(gnome-calculator)$ windowrule = center on, match:class ^(org.gnome.Calculator)$

# ── Opacidade ────────────────────────────────────────────────

windowrule = opacity 0.95 0.85, match:class ^(kitty)$ windowrule = opacity 0.98 0.95, match:class ^(Code)$ windowrule = opacity 1.0 override 1.0 override, match:class ^(google-chrome)$ windowrule = opacity 1.0 override 1.0 override, match:class ^(vlc)$

# ── Workspace por app ────────────────────────────────────────

windowrule = workspace 2, match:class ^(google-chrome)$ windowrule = workspace 3, match:class ^(Code)$ windowrule = workspace 4, match:class ^(discord)$ windowrule = workspace 4, match:class ^(telegram-desktop)$ windowrule = workspace 5, match:class ^(Spotify)$

# ── Fix XWayland & Misc ──────────────────────────────────────

windowrule = suppress\_event maximize, match:class .\* windowrule = float on, match:class ^(wine)$

# ── Layer Rules (v0.54 exige sintaxe de namespace) ──────────

layerrule = blur on, waybar layerrule = ignore\_alpha 0.3, waybar layerrule = blur on, rofi layerrule = ignore\_alpha 0.3, rofi layerrule = blur on, swaync-control-center layerrule = ignore\_alpha 0.3, swaync-control-center

>>>>>>> e034eec (sadads)
